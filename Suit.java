package BlackJack;

// An Enum (representing the four possible suits of a card). Enum is an integer type called Suit, which we can assign the type to the named variables below.
public enum Suit {
    Clubs,
    Diamonds,
    Spades,
    Hearts,
}